#include "storyserver.h"

int main(void)
{
    message m;
    int i = 0;

        printf("message: ");
        fgets(m.buffer, MESSAGE_LEN, stdin);

        /* write message */


        /* receive story */

        printf("OK\n");

    exit(0);
}
