#include "storyserver.h"

#include <string.h>
#include <signal.h>

int main(void)
{
    message m;
    char* story;


        story = strdup("");

        /* create socket  */

        while(true){
            /* read message */
            /* process message */
            story = strcat(story, m.buffer);

        }

        printf("OK\n");

    exit(EXIT_SUCCESS);
}
